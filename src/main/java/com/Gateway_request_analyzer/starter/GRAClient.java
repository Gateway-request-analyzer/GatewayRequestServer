package com.Gateway_request_analyzer.starter;

public class GRAClient {
  // This class should have an instance of the http client
  public void sendEvent(String ip, String userId, String session, String URI
  ) // We might need more data, but start with this
  {
    // Serialize the event and send it to the GRA server using the http client
  }
}

